import { AddAnimationDirective } from './add-animation.directive';

describe('AddAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new AddAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
